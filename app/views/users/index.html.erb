<div class="indextot">
  <div class= "container">
	<div class="row">	
		<div class="col-xs-3">

			 <%= render 'layouts/styling' %> 

			 <li><%= link_to " Logout", destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out" %> </li>
		</div>
	      <div class="col-xs-9">
	      	<h2>User's all</h2>
	      	  <% @users.each do |user| %>
               <div class="userbox">
                <table>
              	  <tr>
                    <td><%= attachment_image_tag user, :image, format: "jpeg", fallback:"no_image.jpg", size: "200x200" %></td>
                  </tr>
                  <tr>
                    <td><%= link_to user.name, user_path(user.id) %></td>
                  </tr>
                  <tr>
                    <td><%= user.height %> | <%= user.gender %> | <%= user.favorite_brand %></td>
                  </tr>
                </table>
              </div>
              <% end %>
              <div align="right"><%= paginate @users %></div>
             <h2>Women's</h2>
               <% @users1.each do |user| %>
               <div class="userbox">
                <table>
              	  <tr>
                    <td><%= attachment_image_tag user, :image, format: "jpeg", fallback:"no_image.jpg", size: "200x200" %></td>
                  </tr>
                  <tr>
                    <td><%= link_to user.name, user_path(user.id) %></td>
                  </tr>
                   <tr>
                    <td><%= user.height %> | <%= user.gender %> | <%= user.favorite_brand %></td>
                  </tr>
                </table>
              </div>
              <% end %>
               <div align="right"><%= paginate @users1 %></div>
              <h2>Men's</h2>
               <% @users2.each do |user| %>
               <div class="userbox">
                <table>
              	  <tr>
                    <td><%= attachment_image_tag user, :image, format: "jpeg", fallback:"no_image.jpg", size: "200x200" %></td>
                  </tr>
                  <tr>
                    <td><%= link_to user.name, user_path(user.id) %></td>
                  </tr>
                   <tr>
                    <td><%= user.height %> | <%= user.gender %> | <%= user.favorite_brand %></td>
                  </tr>
                </table>
                 </div>
              <% end %>
               <div align="right"><%= paginate @users2 %></div>
        </div>
      </div>
    </div>
</div>